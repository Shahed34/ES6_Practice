const name="iPhone 14 Pro Max";
let price=258025;

const myPhone=`My phone name ${name} 
and Price ${price}`;
console.log(myPhone);

const person={
    name:"abdullah",
    class:"12",
    subject:"C Programming"
}

const details=`${person.name} study at ${person.class}.He taken subject is ${person.subject}`;
console.log(details);